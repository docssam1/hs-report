# 학부모용 파이널 진단 리포트

2026-09-12 · 설계: Astra · 구현: Sol · 적용 범위: 파이널 성적표

## 읽는 순서

학부모가 이번 결과, 누적 위치, 잘하는 영역, 먼저 공부할 문제를 차례로 확인한다. 저장된 선생님 코멘트를 상단에 두고, 자동 장문 조언은 별도 펼침 영역으로 제공한다.

왼쪽 색인: 성적 요약 / 강점과 보완점 / 배점대별 결과 / 문항별 진단 / 오답 복습 / 교재 연결 / 상세 답안. 모바일에서는 상단 가로 색인으로 바꾼다. 색인 이동은 접힌 대상도 펼치고 현재 위치를 표시한다.

## 이번 회차와 누적 비교

- 파이널 2회부터 이번 회차와 파이널 1~현재 회차 누적을 나란히 표시한다. 포함 회차를 함께 적는다.
- 점수는 이번 점수와 포함 회차 평균 점수, 석차는 이번 석차 백분율과 회차별 석차 백분율 평균을 구분한다. 누적 등급은 기존 승인된 누적 기준표로 판정한다.
- 공식 누적에는 회차별 최초 응시만 포함한다. 재응시는 이번 연습 결과로 구분한다. 이후 회차 기록은 해당 회차까지의 비교에 넣지 않는다.
- 누적 영역·배점대 득점률은 포함 문항의 득점 합 / 배점 합이다. 회차별 득점률의 단순 평균을 쓰지 않는다.
- 비교 차이는 이번−누적의 퍼센트포인트다. 이를 곧바로 성장이나 하락으로 판정하지 않는다.
- 답안이 있는 회차와 석차 자료가 있는 회차가 다르면 반영 회차를 각각 표시한다. 미등록·불러오기 실패·자료 부족을 0점이나 0%로 바꾸지 않는다.
- 강점은 기존 2문항 이상·득점률 70% 이상 기준을 따른다. 한 문항만으로 지속적인 강점·약점을 판정하지 않는다.
- 반복 약점은 승인된 유형 식별자와 서로 다른 회차로 센다. 같은 회차의 동일 유형 여러 문제를 여러 회차처럼 세지 않는다.

## 정보 위계

성적 비교, 영역별 비교, 우선 복습 최대 3문항, 개인 학습계획은 바로 보인다. 기존 레이더와 세부 집단 평균, 판정 기준, 긴 자동 조언, 재응시 이력은 펼쳐 볼 수 있다. 문항별 진단은 배점대별 결과 바로 아래에 두고 30문항 정답률과 우선 복습 표시를 유지한다.

우선 복습은 잘 맞히는 영역에서 놓친 문제 중 정답률이 특히 높은 문제를 우선한다. 후보가 없으면 별표 추천을 만들어 내지 않는다. 예상 회복 점수는 선택한 오답을 모두 맞히고 다른 답안이 같을 때의 목표임을 짧게 밝힌다.

응시인원은 공개하지 않는다. 석차 백분율을 합격확률로 표현하지 않는다. 학생 답안·고정 정답률·교사 코멘트 저장 정책은 변경하지 않는다.

## 화면과 인쇄

흰색 본문, 짙은 글자, 파란 주 행동, 충분한 여백을 사용한다. 기존 성적표 안에 긴 설명을 추가하는 대신 비교표와 짧은 문장으로 핵심을 보여준다. 1280px와 390px에서 검수한다.

화면 색인은 인쇄에서 숨긴다. 접힌 진단 내용은 인쇄 시 모두 펼친다. 표지·진단·교재 연결·상세답안을 한 세트로 출력하며 워터마크와 답안 시작 면 규칙을 유지한다.

## 검증

정상 1~2회, 이후 회차 기록, 중간 회차 미등록, 현재 재응시, 일부 석차 자료 부족, 동일 회차 유형 중복, 모든 문제 정답, 긴 교사 코멘트를 확인한다. 운영 학생 데이터 쓰기 없이 합성 기록으로 계산·메뉴·모바일·인쇄를 검증한다.
