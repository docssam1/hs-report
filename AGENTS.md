# 황소 초등 대비 작업 규칙

- 문항 제작, 정답 계산, 난이도 설계, 원본 대조는 `S0L / 울트라`로 수행한다. 원본과 문항번호·조건·그림·정답을 1:1 대조하고, 별도 계산으로 정답의 유일성을 검산한다.
- 여러 파일 수정, 데이터 연결, 기존 기능 유지와 배포는 `S0L / 높은`으로 수행한다. `git status`, diff, 테스트, 화면 검사를 마친 뒤에만 배포한다.
- 단순 문구·CSS·반복 목록화·비파괴 검사에는 `LUNA` 또는 `TERRA / 낮음~중간` 에이전트를 사용할 수 있다. 고난도 수학 문항의 최종 판단을 낮은 설정 에이전트에 맡기지 않는다.
- 남은 토큰이 10%에 도달하면 새 문항 생성·배포·병합을 중단한다. 당시의 진행 상황, 검수 완료 항목, 검수 대기 문항과 다음 검수 절차를 남긴다.
- 원본 자료와 사용자 변경은 삭제·초기화·덮어쓰지 않는다. 실패·불확실 문항은 공개하지 않고 `검수 대기`로 잠근다.
