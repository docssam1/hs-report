# 황소 초등 대비 작업 규칙

- **문제은행 필수 읽기:** 새 유사문제 제작·기존 문항 수정·그림/폰트/인쇄 검수 전 `QUESTION-BANK-QUALITY-STANDARD.md`를 읽는다. 원문/변형의 조건·사고 유형·난이도 관계, 정답 기준(복수 정답 포함), 독립 검산, 상세 풀이, 그림·출력·연결을 각각 검수한다. 기존 수학 검수나 해시 일치를 새 기준 전체 통과로 간주하지 않는다.

- **문제은행 최상위 제작 기준:** 문제 아래에 ①②③ 형식의 조건 요약, 지문 반복, 풀이 순서, 읽기 도움말, 힌트를 덧붙이지 않는다. 문제를 푸는 데 필요한 조건은 자연스러운 문제 지문 안에 넣는다. 수열, 표, 실제 단서, 선택지처럼 문제 자체의 필수 자료만 `주어진 자료`로 분리할 수 있으며, 이때도 지문을 다시 요약하거나 물음을 반복하지 않는다. 새 문제은행과 기존 문제은행 수정에 모두 먼저 적용한다.

- 지필드 학습 UI·시험지 디자인 작업은 `DESIGN.md`와 공용 `gfield-design-system` 스킬을 확인한다. 공용 스타일 적용은 요청한 화면에 한정하고 수학·권한 검수를 대신하지 않는다.

- 원본형 모의고사·로드맵 후속 작업은 시작 전에 저장소 루트의 `CLAUDE_HANDOFF.md`를 읽고, 기록된 실제 원본·검증·보류 경계를 확인한다.
- 문항 제작, 정답 계산, 난이도 설계, 원본 대조는 `S0L / 울트라`로 수행한다. 원본과 문항번호·조건·그림·정답을 1:1 대조하고, 별도 계산으로 정답의 유일성을 검산한다.
- 여러 파일 수정, 데이터 연결, 기존 기능 유지와 배포는 `S0L / 높은`으로 수행한다. `git status`, diff, 테스트, 화면 검사를 마친 뒤에만 배포한다.
- 단순 문구·CSS·반복 목록화·비파괴 검사에는 `LUNA` 또는 `TERRA / 낮음~중간` 에이전트를 사용할 수 있다. 고난도 수학 문항의 최종 판단을 낮은 설정 에이전트에 맡기지 않는다.
- 남은 토큰이 10%에 도달하면 새 문항 생성·배포·병합을 중단한다. 당시의 진행 상황, 검수 완료 항목, 검수 대기 문항과 다음 검수 절차를 남긴다.
- 원본 자료와 사용자 변경은 삭제·초기화·덮어쓰지 않는다. 실패·불확실 문항은 공개하지 않고 `검수 대기`로 잠근다.
