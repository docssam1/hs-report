# 지필드 디자인·UX 적용 기록

공용 기준: `gfield-design-system` v1.0 (2026-09-06).
로컬 원본: `E:/Codex/skills/gfield-design-system/SKILL.md`와 `references/design-rules.md`.

이번 적용 범위는 파이널 1회 약점 유형 학습지다. 다른 화면이나 프로젝트에 전역 스타일을 덮어쓰지 않는다.

## 학생의 업무

자신의 오답에 연결된 시험지를 바로 열고, 선택한 배점대의 문제를 공부·인쇄한다. 교사용 유형 검색이나 내부 생성 설정을 반복하지 않는다.

```text
파이널 1회 약점 유형
전체문제 | 2점대 | 3점대 | 4점대     인쇄 구성 [둘 다] [인쇄]
┌─────────────────────────────────────┐
│ [학생 이름] 학생의                    │
│ 파이널 1회 약점 유형                  │
│ □ 원문 4번 · 구체적인 유형명          │
│                    □ 원문 7번 · 유형 │
│ 한 줄 두 유형, 해결 여부 셀프 체크    │
└─────────────────────────────────────┘
문제 2단 × 3행 → 별도의 정답 및 풀이
```

## 시각 계약

흰 종이 `#FFFFFF`, 작업 배경 `#F5F6F8`, 글자 `#182230`, 보조 글자 `#566274`, 구분선 `#D8DEE8`, 주 행동 `#2456C4`를 사용한다. 주황 버튼·누런 표지·그라데이션·장식용 영어 표어를 넣지 않는다.

본문은 한글 산세리프, 주요 표지 제목은 명조/바탕 계열로 구분한다. 조작부는 터치와 키보드로 사용할 수 있어야 한다. 교재는 카드형 대시보드가 아닌 실제 종이 페이지로 보인다.

## 콘텐츠·인쇄 계약

- 원문별 고정 3문항을 유형 사이에 섞는다. 배점 전환은 처음 받은 오답 범위를 넓히지 않는다.
- 문제의 인쇄 번호와 정답 번호는 같은 선택 결과에서 만들어 일치시킨다. 원문 번호는 별도로 표시한다.
- 문제만 / 답안·풀이만 / 둘 다 / 빠른 정답만의 미리보기와 인쇄를 일치시킨다.
- 워터마크는 표지·문제·빠른 정답·답안과 풀이 모두 필수다. 이름이 없더라도 지필드 워터마크를 표시한다. 양면 인쇄용 빈 면만 제외하며 본문 가림이나 추가 페이지를 만들지 않는다.
- 함께 인쇄 시 표지를 포함한 문제 부분이 홀수 페이지이면 빈 면 하나를 넣어 답안이 새 종이 앞면에서 시작한다.
- 2단 6문항의 세 행 높이를 같게 하고 문항 시작은 각 칸 위에 맞춘다.
- 셀프 체크는 개인 점검 표시이며 성적을 수정하지 않는다. 학생 이름은 표시 정보이지 권한이 아니다.
- 실제 390px·데스크톱·페이지 단위 PDF를 검수한다. 내용 감사와 UI 감사가 모두 끝나기 전 배포 완료로 보고하지 않는다.

공개 참고: [매쓰플랫](https://www.mathflat.kr/), [캐츠 문제은행](https://teach.cats4me.net/question-bank/math/my-test-papers), [공개 frontend-design 스킬](https://github.com/anthropics/skills/tree/41bbe19d1a1a7eaab5e7bb9050a417e5c6cffc8f/skills/frontend-design). 회원 전용 기능은 검증하지 않았으며 로고·문제·화면 코드는 가져오지 않았다.
